import React, {
  useEffect,
  useState
} from 'react';

export const useDownloader = () => {
  const [resp, setResp] = useEffect("");

  useEffect(() => {

  }, resp, setResp);

  return resp;
}

export default useDownloader;